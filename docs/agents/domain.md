# Domain Documentation

This repository follows a **single-context** layout.

- `CONTEXT.md` is located in the repository root.
- `docs/adr/` contains Architectural Decision Records.

## Metadata (for `agent-guide --json`)
```json
{
  "type": "domain-docs",
  "layout": "single-context",
  "paths": {
    "context": "./CONTEXT.md",
    "adr": "./docs/adr/"
  }
}
```
