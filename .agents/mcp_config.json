{
  "mcpServers": {
    "just3ws-site": {
      "command": "node",
      "args": ["bin/mcp_server.js"],
      "description": "Repository-native self-describing MCP Server exposing corpus validation tools, skill specs, and resource endpoints."
    }
  }
}
