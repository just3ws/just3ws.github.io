version: 1
name: transcript-import-batch
metadata:
  display_name: Transcript Import Batch
  short_description: Batch ingest transcripts from outbox with report + safe apply.
  default_prompt: Run transcript outbox dry-run, review mapping confidence, apply high-confidence mappings, and validate.
