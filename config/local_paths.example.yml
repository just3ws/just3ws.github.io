# Copy to config/local_paths.yml for machine-specific paths.
# This example is intentionally symbolic and safe to commit.
archive_root: "$ARCHIVE_ROOT"
witc_corpus_dir: "$WITC_CORPUS_DIR"
vimeo_videos_dir: "$VIMEO_VIDEOS_DIR"
vimeo_outbox_dir: "$VIMEO_OUTBOX_DIR"
chatgpt_dump_dir: "$CHATGPT_DUMP_DIR"
