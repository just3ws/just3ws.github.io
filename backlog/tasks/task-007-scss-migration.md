# Task-007: CSS to SCSS/SASS Migration

**Status:** To Do
**Priority:** Medium

## Description
Current CSS uses `@import` which has performance impacts and limits styling flexibility. Migrating to SCSS will allow for better variables, nesting, and organization.

## Acceptance Criteria
- [ ] Rename existing `.css` files to `.scss`.
- [ ] Move shared styles into a `_sass/` directory.
- [ ] Implement variables for colors (using existing Nord theme), typography, and spacing.
- [ ] Ensure compiled CSS output matches the previous styling.
