---
timezone: America/Chicago

# Canonical host (used by jekyll-sitemap, jekyll-seo-tag, feeds, etc.)
url: https://www.just3ws.com
baseurl: ""
title: Mike Hall
description: Staff-level software engineer with expertise in Ruby, Rails, PostgreSQL, and scalable backend systems.
release_timestamp: 2026-02-16T17:40:57Z
author:
  name: Mike Hall
  email: mike@just3ws.com
social:
  name: Mike Hall
  links:
    - https://www.linkedin.com/in/just3ws/
    - https://github.com/just3ws

plugins:
  - jekyll-feed
  - jekyll-include-cache
  - jekyll-redirect-from
  - jekyll-seo-tag
  - jekyll-sitemap

sass:
  sass_dir: _sass
  style: compressed

defaults:
  - scope:
      path: ""
    values:
      sitemap: true
      robots: index,follow
  # Docs are public by default; operator runbooks are explicitly noindex below.
  - scope:
      path: "docs"
    values:
      sitemap: true
      robots: index,follow
  # Operator/runbook docs that are useful for maintainers but noisy for search.
  - scope:
      path: "docs/architecture/transcript-import.md"
      type: "pages"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/domain-migration.md"
      type: "pages"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/linkedin-import"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/pipeline-grammar.md"
      type: "pages"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/interview-data-model.html"
      type: "pages"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/repo-hygiene.md"
      type: "pages"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/resources"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/taxonomy"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/archive-philosophy"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/retrospectives"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/site-tree"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/tasks.md"
      type: "pages"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/video-metadata-completeness"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "docs/wayback"
    values:
      sitemap: false
      robots: noindex,follow
  # Utility/ops surfaces outside docs.
  - scope:
      path: "tools"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "archive-status"
    values:
      sitemap: false
      robots: noindex,follow
  - scope:
      path: "linkedin.html"
      type: "pages"
    values:
      sitemap: false
      robots: noindex,follow

exclude:
  # Ruby / build tooling
  - ".bundle"
  - "Gemfile"
  - "Gemfile.lock"
  - "Rakefile"
  - ".tool-versions"

  # Editor / IDE
  - ".editorconfig"

  # Environment
  - ".env"

  # Repo metadata
  - ".gitignore"
  - ".github"
  - "README.md"
  - "LICENSE"

  # Project documentation (not site content)
  - "CLAUDE.md"
  - "CODEX.md"
  - "AGENTS.md"
  - "context"
  - "skills"

  # Local / runtime
  - "bin"
  - "tmp"
  - "working-assets"
  - "node_modules"
  - "vendor"
