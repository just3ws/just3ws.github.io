---
# Register for bin/validate_resume_claims.rb.
#
# `allowed` — the claim is backed by the career data, just not as that literal
# number (a rounding, or a figure derived from dates). Each needs the source.
# `pending` — no source found. Reported as a warning on every run, and stays
# reported until it is sourced, reworded, or pulled. Not silent, not fatal.

allowed:
  "820+": >-
    Rounds down the 823 SCMC members recorded in
    _data/resume/leadership.yml:8. Verified 2026-08-24.
  "25+": >-
    Derived from the dates rather than stated as a number: the earliest
    position (ch-robinson) starts December 1999, so 2026 is 27 years. The
    claim is conservative against its own source. Verified 2026-08-24.

pending: {}
# Resolved 2026-08-24 and removed from this register:
#
#   "36+" — a ghost. The operator identified it as the count of pending
#   platform updates awaiting verification at EMR-Bear, which is what the
#   original sentence said ("release verification gates for 36+ pending
#   platform updates"). Generated prose then mutated it into "36+ services"
#   for the OneMain OpenTelemetry rollout and "36+ vendors" for supply-chain
#   risk, neither of which it ever meant. Both mutations are pulled, including
#   from the two generator instructions that kept re-injecting it
#   (.agents/skills/executive-brief-generator/SKILL.md and
#   docs/inter-tool-communication-protocol.md). The number is not restored
#   anywhere: the current source data does not carry it.
